# 贡献指南 / Contributing Guide

感谢你对 LLM Data Lab 项目的关注！我们欢迎任何形式的贡献。

## 🤝 如何贡献

### 报告问题 (Issues)
- 使用 GitHub Issues 报告 bug 或提出功能建议
- 请提供清晰的问题描述和复现步骤
- 如果可能，附上错误日志或截图

### 提交代码 (Pull Requests)

1. **Fork 本仓库**
   ```bash
   # 在 GitHub 上点击 Fork 按钮
   ```

2. **克隆到本地**
   ```bash
   git clone https://github.com/你的用户名/llm_stats_web.git
   cd llm_stats_web
   ```

3. **创建功能分支**
   ```bash
   git checkout -b feature/your-feature-name
   # 或
   git checkout -b fix/your-bug-fix
   ```

4. **进行开发**
   - 遵循现有的代码风格
   - 添加必要的注释
   - 确保代码能够正常运行

5. **提交更改**
   ```bash
   git add .
   git commit -m "feat: 添加某某功能"
   # 或
   git commit -m "fix: 修复某某问题"
   ```

6. **推送到 GitHub**
   ```bash
   git push origin feature/your-feature-name
   ```

7. **创建 Pull Request**
   - 在 GitHub 上打开你的分支
   - 点击 "New Pull Request"
   - 描述你的修改内容

## 📝 代码规范

### Python (后端)
- 遵循 PEP 8 规范
- 使用 `ruff` 进行代码格式化和检查
- 函数和类添加 docstring

```bash
# 运行 linter
cd backend
ruff check .
```

### TypeScript/React (前端)
- 遵循 ESLint 配置
- 使用 TypeScript 类型注解
- 组件使用函数式写法

```bash
# 运行 linter
cd frontend
npm run lint
```

### 提交信息格式
建议使用以下格式：
- `feat: 添加新功能`
- `fix: 修复 bug`
- `docs: 更新文档`
- `style: 代码格式调整`
- `refactor: 代码重构`
- `test: 添加测试`
- `chore: 构建/工具相关`

## 🧪 测试

在提交 PR 前，请确保：
- [ ] 后端代码能正常启动
- [ ] 前端代码能正常构建
- [ ] 新功能经过手动测试
- [ ] 没有引入新的 linter 错误

```bash
# 后端测试
cd backend
pytest

# 前端构建测试
cd frontend
npm run build
```

## 🌟 开发环境设置

详见 [README.md](README.md) 的"快速启动"部分。

## 🔒 安全注意事项

- **永远不要**提交包含 API Key 或密码的代码
- **永远不要**提交真实的用户数据
- 使用 `.env.example` 作为配置示例
- 敏感信息应通过环境变量传递

## 📧 联系方式

如有疑问，欢迎：
- 提交 Issue
- 发起 Discussion
- 提交 Pull Request

再次感谢你的贡献！🎉

