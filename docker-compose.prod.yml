# Docker Compose 生产环境配置覆盖文件
# 
# ⚠️  注意：
# - API 地址现在通过构建参数传递（见 frontend/Dockerfile）
# - 使用 deploy.sh 会自动配置，无需手动设置
# - 此文件保留用于手动部署的向后兼容
#
# 使用方法：
#   bash deploy.sh start cn prod  # 推荐：自动配置
#   或手动：docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  backend:
    environment:
      # 🔒 生产环境 CORS 配置
      # 📝 部署到自己的域名时，修改下面的域名
      - ALLOWED_ORIGINS=https://btchuro.com,https://www.btchuro.com

  frontend:
    environment:
      # 📝 部署到自己的域名时，修改下面的域名
      - NEXT_PUBLIC_API_BASE_URL=https://btchuro.com/api

